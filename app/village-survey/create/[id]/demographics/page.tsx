import React from 'react'
import PropTypes from 'prop-types'
import { CreateDemographicsForm } from '@/src/screens/form'

const page = () => {
  return (
    <div>
        <CreateDemographicsForm/>
    </div>
  )
}

page.propTypes = {}

export default page